// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

event CreatePool(address);

event ValtzPoolStart(uint40 startTime);

event ValtzPoolDeposit(address indexed depositor, uint256 amount);

event ValtzPoolRedeem(address indexed redeemer, uint256 amount);
